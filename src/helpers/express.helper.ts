import { Router, Request, Response, NextFunction } from "express";
import express from "express";
import cors from "express";

const route = Router();

export { route, Request, Response, express, cors, NextFunction };
